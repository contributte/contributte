# Web Application

A consumer-ready starter for a Nette web application.

## Installation

```bash
composer create-project contributte/web-application app
cd app
make init
```

## Usage

```bash
make dev
```

Open [http://localhost:8000/health](http://localhost:8000/health). It returns `{"status":"ok"}`.
